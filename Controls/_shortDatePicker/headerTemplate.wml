<div class="controls-PeriodLiteDialog__currentValue"
     title="{[Текущий период]}">
    <ws:if data="{{ contentTemplate }}">
        <ws:partial startValue="{{ startValue }}" endValue="{{ endValue }}" template="{{ contentTemplate }}"/>
    </ws:if>
    <ws:else>
        {{caption}}
    </ws:else>
</div>
