<Controls.decorator:Number
        attr:class="{{fontColorStyle ? 'controls-text-' + fontColorStyle}}
        {{fontSize ? 'controls-fontsize-' + fontSize}}"
        value="{{value}}"
        scope="{{displayTypeOptions}}"/>
