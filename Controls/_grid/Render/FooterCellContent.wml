<div if="{{ !!content || !!contentTemplate }}"
     class="{{ column.getContentClasses(theme, backgroundColorStyle, cursor, highlightOnHover) }}"
     style="{{ column.getContentStyles() }}">
    <div class="{{ column.getInnerContentWrapperClasses() }}">
        <ws:partial template="{{ content || contentTemplate }}" scope="{{ _options }}"/>
    </div>
</div>
