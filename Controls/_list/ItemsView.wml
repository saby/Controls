<div>
   <Controls._list.BaseControl
      name="baseControl"
      scope="{{_options}}"

      useNewModel="{{true}}"
      viewName="Controls/list:ListView"
      viewModelConstructor="Controls/display:Collection"

      source="{{_source}}"
      keyProperty="{{_keyProperty}}"
      sourceController="{{_sourceController}}"
   />
</div>
