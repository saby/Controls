<ws:if data="{{column.config.result }}">
   <Controls.decorator:Money value="{{column.result}}"
                             fontColorStlye="results"
                             fontSize="l"
                             fontWeight="bold"
                             useGrouping="{{true}}"/>
</ws:if>
<ws:else>
<invisible-node />
</ws:else>
