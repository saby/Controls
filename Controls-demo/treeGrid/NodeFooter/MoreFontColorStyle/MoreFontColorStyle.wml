<div class="controlsDemo__wrapper controlsDemo__flexRow">
    <div class="controlsDemo__cell controlsDemo__maxWidth500">
        <div class="controls-text-label">default</div>
        <Controls.treeGridOld:View
                name="tree1"
                keyProperty="id"
                source="{{_viewSource}}"
                columns="{{_columns}}"
                bind:expandedItems="_expandedItems1"
                parentProperty="parent"
                nodeProperty="type"
                useNewNodeFooters="{{ true }}"
                navigation="{{_navigation}}"/>
    </div>
    <div class="controlsDemo__cell controlsDemo__maxWidth500">
        <div class="controls-text-label">moreFontColorStyle=link</div>
        <Controls.treeGridOld:View
                name="tree2"
                keyProperty="id"
                source="{{_viewSource}}"
                columns="{{_columns}}"
                bind:expandedItems="_expandedItems2"
                parentProperty="parent"
                nodeProperty="type"
                useNewNodeFooters="{{ true }}"
                navigation="{{_navigation}}"
                moreFontColorStyle="link"/>
    </div>
    <div class="controlsDemo__cell controlsDemo__maxWidth500">
        <div class="controls-text-label">moreFontColorStyle=primary</div>
        <Controls.treeGridOld:View
                name="tree3"
                keyProperty="id"
                source="{{_viewSource}}"
                columns="{{_columns}}"
                bind:expandedItems="_expandedItems3"
                parentProperty="parent"
                nodeProperty="type"
                useNewNodeFooters="{{ true }}"
                navigation="{{_navigation}}"
                moreFontColorStyle="primary"/>
    </div>
    <div class="controlsDemo__cell controlsDemo__maxWidth500">
        <div class="controls-text-label">moreFontColorStyle=secondary</div>
        <Controls.treeGridOld:View
                name="tree4"
                keyProperty="id"
                source="{{_viewSource}}"
                columns="{{_columns}}"
                bind:expandedItems="_expandedItems4"
                parentProperty="parent"
                nodeProperty="type"
                useNewNodeFooters="{{ true }}"
                navigation="{{_navigation}}"
                moreFontColorStyle="secondary"/>
    </div>
    <div class="controlsDemo__cell controlsDemo__maxWidth500">
        <div class="controls-text-label">moreFontColorStyle=success</div>
        <Controls.treeGridOld:View
                name="tree5"
                keyProperty="id"
                source="{{_viewSource}}"
                columns="{{_columns}}"
                bind:expandedItems="_expandedItems5"
                parentProperty="parent"
                nodeProperty="type"
                useNewNodeFooters="{{ true }}"
                navigation="{{_navigation}}"
                moreFontColorStyle="success"/>
    </div>
    <div class="controlsDemo__cell controlsDemo__maxWidth500">
        <div class="controls-text-label">moreFontColorStyle=warning</div>
        <Controls.treeGridOld:View
                name="tree6"
                keyProperty="id"
                source="{{_viewSource}}"
                columns="{{_columns}}"
                bind:expandedItems="_expandedItems6"
                parentProperty="parent"
                nodeProperty="type"
                useNewNodeFooters="{{ true }}"
                navigation="{{_navigation}}"
                moreFontColorStyle="warning"/>
    </div>
    <div class="controlsDemo__cell controlsDemo__maxWidth500">
        <div class="controls-text-label">moreFontColorStyle=danger</div>
        <Controls.treeGridOld:View
                name="tree7"
                keyProperty="id"
                source="{{_viewSource}}"
                columns="{{_columns}}"
                bind:expandedItems="_expandedItems7"
                parentProperty="parent"
                nodeProperty="type"
                useNewNodeFooters="{{ true }}"
                navigation="{{_navigation}}"
                moreFontColorStyle="danger"/>
    </div>
    <div class="controlsDemo__cell controlsDemo__maxWidth500">
        <div class="controls-text-label">moreFontColorStyle=unaccented</div>
        <Controls.treeGridOld:View
                name="tree8"
                keyProperty="id"
                source="{{_viewSource}}"
                columns="{{_columns}}"
                bind:expandedItems="_expandedItems8"
                parentProperty="parent"
                nodeProperty="type"
                useNewNodeFooters="{{ true }}"
                navigation="{{_navigation}}"
                moreFontColorStyle="unaccented"/>
    </div>
    <div class="controlsDemo__cell controlsDemo__maxWidth500">
        <div class="controls-text-label">moreFontColorStyle=label</div>
        <Controls.treeGridOld:View
                name="tree9"
                keyProperty="id"
                source="{{_viewSource}}"
                columns="{{_columns}}"
                bind:expandedItems="_expandedItems9"
                parentProperty="parent"
                nodeProperty="type"
                useNewNodeFooters="{{ true }}"
                navigation="{{_navigation}}"
                moreFontColorStyle="label"/>
    </div>
    <div class="controlsDemo__cell controlsDemo__maxWidth500">
        <div class="controls-text-label">moreFontColorStyle=info</div>
        <Controls.treeGridOld:View
                name="tree10"
                keyProperty="id"
                source="{{_viewSource}}"
                columns="{{_columns}}"
                bind:expandedItems="_expandedItems10"
                parentProperty="parent"
                nodeProperty="type"
                useNewNodeFooters="{{ true }}"
                navigation="{{_navigation}}"
                moreFontColorStyle="info"/>
    </div>
    <div class="controlsDemo__cell controlsDemo__maxWidth500">
        <div class="controls-text-label">moreFontColorStyle=default</div>
        <Controls.treeGridOld:View
                name="tree11"
                keyProperty="id"
                source="{{_viewSource}}"
                columns="{{_columns}}"
                bind:expandedItems="_expandedItems11"
                parentProperty="parent"
                nodeProperty="type"
                useNewNodeFooters="{{ true }}"
                navigation="{{_navigation}}"
                moreFontColorStyle="default"/>
    </div>
</div>
